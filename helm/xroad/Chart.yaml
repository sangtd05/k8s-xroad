---
apiVersion: v2
name: xroad
description: A complete X-Road deployment with Central Server and Security Server cluster

type: application
version: 0.1.0
appVersion: "7.6.0"

# Dependencies removed - PostgreSQL Operator installed separately
# This chart now focuses on X-Road components only
