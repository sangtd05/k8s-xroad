# X-Road Signer Configuration
# Basic configuration for X-Road Signer service

# Database configuration
db.host=cs-postgres-service
db.port=5432
db.name=xroad_cs
db.user=admin
db.password=admin123

# Signer configuration
signer.port=5588
signer.host=0.0.0.0

# Logging configuration
logging.level=INFO
logging.file=/var/log/xroad/signer.log

# Security configuration
security.enabled=true
